// Sound effects are disabled. Re-enable by restoring the Web Audio API implementation below.

export const playToggleSound = () => {};

export const playHoverSound = () => {};

export const playClickSound = () => {};
